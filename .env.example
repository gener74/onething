# Clau de l'API de Claude per a la funció serverless /api/breakdown.
# Còpia aquest fitxer a .env.local (ignorat per git) i posa-hi la teva clau,
# o configura-la com a variable d'entorn al projecte de Vercel.
ANTHROPIC_API_KEY=
